# Next.js Example App

This example uses the Next.js App Router.

## Local Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

Variables prefixed with `NEXT_PUBLIC_` are exposed to browser code. Keep private secrets out of client components.
