# React Example App

This example uses React with Vite so learners can practice a common frontend deployment flow.

## Local Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

Only `VITE_` variables are available in browser code.
