# Vite Example App

This example demonstrates a small Vite deployment surface.

## Local Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

Copy `.env.example` to `.env` for local experiments.

Only variables prefixed with `VITE_` are exposed to browser code.
